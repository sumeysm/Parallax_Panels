This app lets you create full page slideshows by combining images, text markup, and animated transitions.

Todo: after the slideshow has been created, the app should be able to produce a monolithic HTML file that replicates the slideshow without the cpontrol panel overlay
